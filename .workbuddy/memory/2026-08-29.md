# 2026-08-29 工作日志

## 性能专项 review（只读，未改代码）
- 报告存档 design/performance-review-2026-08-29.md，基线 main @16efa65，全量源码 4496 行
- 8 项：PERF-1 高（转写请求体主线程构造，P1-7 只修了一半：静音裁剪已 detached，body 拼接仍在 @MainActor 的 transcribeAudioFileWithAPI:1405→1827→1852）；PERF-2 每次转写重算系统语音区域（1872/1899，supportedLocales+Locale 构造在 MainActor）；PERF-3 电平每 21ms 投主线程 Task（1093/1158→404，约 47 次/秒）；PERF-4 键盘事件 global+local 双监听重复处理（323-330）；PERF-5 统计页 dictionaryRepresentation 全量扫描（DictationStats:36）；PERF-6 日志每条 open/close；PERF-7 限流器 release fire-and-forget；PERF-8 HUD TimelineView 每帧重绘
- 子 agent 派发两次均 429 失败，改为自己逐点 grep+读码核实（每条都带行号）
- 待用户决定修哪些；修之前先切分支

## PERF-1~4 修复（已合并 main）
- 用户确认"一起做"，PERF-1 选方案 A（后台拼 body，非流式上传）。切 fix/perf-20260829 分支，4 项一起做。
- PERF-1：transcribeAudioFileWithAPI 仍 @MainActor，新增 nonisolated static buildTranscriptionRequest（Task.detached(priority:.utility)）把 Data(contentsOf:)+append 挪后台，主线程只 await URLRequest；multipartTranscriptionBody 改 nonisolated static。补全 P1-7 后半段（松手卡顿根因）。
- PERF-2：新增 static let cachedSupportedLocaleIdentifiers 缓存 SFSpeechRecognizer.supportedLocales()，supportedSystemSpeechLocaleIdentifier 改用缓存。
- PERF-3：API 与本地两条录音 tap 各加 OSAllocatedUnfairLock<Double>(uncheckedState:0) 节流，电平投递从 ~45 次/秒降到每 40ms 一次（~25Hz）。注意 OSAllocatedUnfairLock 初始化器是 init(uncheckedState:) 不是 init(initialValue:)，否则编译报 "no exact matches in call to initializer"。
- PERF-4：HotkeyInputManager 新增 shouldHandleEvent（按 类型+keyCode+modifierFlags+timestamp 去重，50ms 窗口），global+local 双 monitor 重复投递只处理一次，避免 toggle 模式一次按下被 start 再 stop。
- 构建：./build-app.sh（--disable-sandbox）一次通过，产物 SoundIn.app（已 rename，非 VoiceScribe.app）。注意 tail -40 的 exit 0 是 tail 的，必须用 grep error: 判断真实构建结果。
- 合并：--no-ff 合回 main，merge commit 0504a0c。

## 单击/长按两块独立触发（已合并 main）
- 需求澄清过程：最初理解为"单键按时长区分"，用户纠正为**两个独立快捷键**（两块设置区域：单击块 / 长按块+阈值）；录制入口就是点击快捷键显示区本身，无单独录制按钮、无"点击此处可录制"提示。
- 实现：HotkeyInputManager 拆 `clickShortcut`(单击切换) + `holdShortcut`(长按说话)，删 TriggerMode/triggerMode；Carbon 注册两个热键 id=1(click)/id=2(hold)，handler 按 EventHotKeyID.id 分派 handleTrigger(kind:)/handleHotKeyRelease(kind:)；新增 press(_ kind:)/release(_ kind:) 统一分派 + isHoldActive 会话标记（stop/cancel/release 均复位）；纯修饰键手势拆 clickModifierArmed/holdModifierArmed，handleModifierGesture(armed:inout:)；load/save 换 voiceInputClickShortcut/voiceInputHoldShortcut 键（旧 voiceInputShortcut 不再读，老用户回默认键）。
- 设置页：generalPage 移除触发方式 Picker（留一行指引到快捷键页）；hotkeysPage 拆「单击触发」「长按触发」两 Section，长按块含长按阈值 Picker(0.5/0.7/1.0)；capture 用 capturingSlot: Slot? 区分目标，onComplete 按 slot 写回 clickShortcut/holdShortcut；displayShortcut=clickShortcut（状态栏只显单击键）。
- 分支 fix/trigger-two-shortcuts，commit 9cced9f，--no-ff 合并 b0080e8；构建通过、应用已重启。
- 遗留提醒：两键若配成同一个组合键行为未定义（未做冲突校验）；老用户的旧触发方式设置不迁移。

## 设置页精简（同日下午）
- 用户反馈：通用页那块「语音输入」说明删除；快捷键页每块下的授权/接管橙色提示只保留**一处**、放最底部。
- 实现：generalPage 删除 Section("语音输入")；hotkeysPage 移除每块内 shortcutWarnings，新增 consolidatedShortcutWarnings（合并 click/hold 两个 shortcut、去重）只在底部 Section 渲染一次。
- 分支 fix/trigger-settings-cleanup，commit ee81fc1，--no-ff 合并 2c92278，已构建重启。

## 通用页 footer 提示移除
- 用户要求：通用页的单击/长按键说明也删掉，仅快捷键页保留提示。移除 generalPage「开机时启动」Toggle 的 footer。
- 分支 fix/trigger-general-footer，commit b3135b4，--no-ff 合并 315ba97，已构建重启。（注意：merge 第一次误在功能分支自身合并导致 main 未更新，已切回 main 重合并）

## 去掉句末标点开关（fix/trigger-strip，已合并 main）
- 需求：转写结果句末的终止标点（。！？!?…）可一键去掉。设计确认：默认关闭、只剥最后一个。
- 实现：HotkeyInputManager.stripTrailingPunctuation（UserDefaults voiceInputStripTrailingPunctuation，默认 false）；stop() 在全部标点跟随逻辑之后、AX 权限检查之前，仅当 trimmedText.last 是终止标点时 dropLast() 一次。
- 坑：该逻辑位于 weak-self 闭包内，直接访问实例属性报 "explicit use of 'self' is required when 'self' is optional" → 改为 `self?.stripTrailingPunctuation == true`。
- 设置页 generalPage 新增 Toggle「去掉转写结果句末标点」，绑 HotkeyInputManager.shared.stripTrailingPunctuation。
- 分支 fix/trigger-strip，commit 8e1821d，--no-ff 合并 5915f07；构建通过、应用已重启（process running）。
- 设计稿 design/strip-trailing-punctuation-mockup.html 留存；废弃的 design/trigger-unified-mockup.html（单键误理解稿）已删。
