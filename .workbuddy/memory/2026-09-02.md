# 2026-09-02

## 状态栏图标粗细统一（fix/menu-icon-uniform-thickness，已合并 main）
- 用户要求：状态栏图标几根线条粗细保持一致，高度保持现状。
- 改动：brandMenuBarIcon 所有竖条宽度统一 1.5pt（原声波条 1.5 / 输入块 2.5 / 光标 1.25 不一致）；高度不变（声波 5/9/13pt、输入块与光标 12pt）；圆角半径随粗细统一为 thickness/2。
- 分支 fix/menu-icon-uniform-thickness，commit 51079ac，--no-ff 合并 d4e4dcb，main 重新打包重启（图标在菜单栏实时生效）。
- 代价：统一粗细后最右「输入块」与「光标」均为 1.5×12 实心黑条，视觉成一对等高双条（原光标更细 1.25 的区分消失）。用户若想保留光标差异需另行处理。

## 状态栏图标颜色统一（fix/menu-icon-solid-color，已合并 main）
- 用户反馈：粗细统一后仍有几根线条显得透明（声波条原本带 1.0/0.8/0.55 渐弱透明度）。
- 根因：图标是 isTemplate 模板图，alpha 即渲染不透明度，渐弱条在菜单栏直接显半透明。
- 改动：brandMenuBarIcon 全部五根竖条改为实心黑（去掉 withAlphaComponent，alpha 统一 1.0）。颜色与粗细均一致。
- 分支 fix/menu-icon-solid-color，commit 30d7537，--no-ff 合并 59c8946，main 重新打包重启。

## 状态栏图标最右光标矮于输入块（fix/menu-icon-cursor-shorter，已合并 main）
- 用户要求：最右一条线高度比第二右一条低一点。
- 改动：brandMenuBarIcon 光标高度 12→10pt（blockH=384*k=12，cursorH=320*k=10），比输入块低 2pt；粗细/颜色/其余高度不变。
- 分支 fix/menu-icon-cursor-shorter，commit bc7d0c3，--no-ff 合并 main，重新打包重启。
