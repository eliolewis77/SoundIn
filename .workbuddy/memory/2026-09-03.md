# 2026-09-03

## 设置页侧边栏折叠按钮改为左上常驻（fix/settings-sidebar-left-toggle，已合并 main）
- 用户对比系统「备忘录」：其折叠按钮固定在窗口左上，展开/收起都不动；而本 App 的 NavigationSplitView 默认 toggle 落在右上角且只在收起后出现。
- 改法：移除 NavigationSplitView，改用手动 HStack（条件显示侧边栏 List + 详情 Form）；新增 @State sidebarVisible，左上 .toolbar 按钮（sidebar.left 图标）调用 toggle 切换。去掉系统默认右上箭头。
- 代价：失去 NavigationSplitView 原生的可拖拽分隔条/列宽记忆；侧边栏宽度改为固定 frame(min130/ideal148/max176)。设置页场景可接受。
- 分支 fix/settings-sidebar-left-toggle，commit 6d63de7，--no-ff 合并 main，重新打包重启。

## 设置页冗余描述文案精简（fix/settings-text-trim，已合并 main）
- 用户要求删减设置页"描述性文本"，选「删冗余、留关键」档。
- 删 5 处：快捷键页录制提示、引擎页"下方字段即当前选中配置…"、优化页用途 footer、优化页"系统默认指令…"说明、录音测试页"说几句话…"提示。
- 保留：快捷键双模式说明、优化页"延迟增加/失败兜底"提示、关于页权限排错提示。
- 分支 fix/settings-text-trim，commit 3d4239e，--no-ff 合并 main，重新打包重启。

## 统计热力图占满宽度、不强制三个月（fix/stats-heatmap-fill-width，已合并 main）
- 用户反馈：热力图应把宽度占满，不用强制显示三个月。
- 改法：DictationHeatmapView 改用 GeometryReader 测可用宽度，反推响应式方形格子尺寸 `cell = (availWidth - weekdayGutter - (cols-1)*gap)/cols`（下限 6pt）；格子不再固定 14pt 居中偏左。左侧星期标签列宽统一为 weekdayGutter=22、HStack 间距归 0，月份标签偏移随动态格子尺寸对齐。
- 统计页 Section 标题由「最近 13 周」改为「最近听写」，去除强制三个月语义；数据仍是 heatmapCells(weeks:13)≈90 天保留期全量。
- 构建两次：首次沙箱拦截 SwiftPM 写 ~/.swiftpm/security（非致命），第二次用 dangerouslyDisableSandbox 跑通 EXIT=0；产物 SoundIn.app 已重新签名。
- 分支 fix/stats-heatmap-fill-width，commit 922059e，--no-ff 合并 main（e78c362），重新打包重启（PID 43402）。

## 热力图在设置页消失（fix/heatmap-geometryreader-height，已合并 main）
- 现象：上一条「占满宽度」改动后热力图不显示。根因：外层 `GeometryReader` 在 Form/Section（底层 List 行）中被提议高度为 0 时，会把自己的高度报成 0，整行高度塌成 0、内容被裁掉。
- 修复：量宽度改为「背景 GeometryReader + WidthKey PreferenceKey」，VStack 仍按内容自然决定行高，宽度在背景单独量取、不参与布局。`cell` 由 measuredWidth 反推（占满一行、方形），未量到时退回最小值兜底。这是 List/Form 做宽度自适应的标准写法。
- 教训：**不要在 SwiftUI Form/List 行里直接把 GeometryReader 当外层布局元素**（会把行高撑成 0）；要量尺寸就用背景/overlay 里的 GeometryReader + PreferenceKey。
- 构建：沙箱拦 SwiftPM 写 ~/.swiftpm/security 导致的误报 failed，实际 EXIT=0、已重新签名。分支 fix/heatmap-geometryreader-height，commit d75fbbd，--no-ff 合并 main（a9445a3），重新打包重启（PID 47799）。

## 回退热力图「占满宽度」改动，恢复原样（fix/heatmap-restore-original，已合并 main）
- 用户反馈修复后格子被压成极小尺寸，要求恢复原样。
- 根因（比上一个坑更深）：背景量宽度的写法存在**循环依赖** —— cell 由 measuredWidth 算出 → measuredWidth 量的是 VStack 宽度 → VStack 宽度又取决于 cell。初始 cell=6pt 时整行约 136pt，量回来仍是 136，重算 cell 还是 6.0，卡死在兜底最小值，格子永远长不到铺满。
- 处理：两个文件回退到改动前的原版（`git checkout f2aaddf -- DictationHeatmapView.swift`；Section 标题改回「最近 13 周」），与原版 35648e7 字节级一致。固定 cellSize=14、gap=3，无 GeometryReader。
- 教训已沉淀进 MEMORY.md「SwiftUI 设置页布局坑」：Form/List 行里不要外层放 GeometryReader（行高塌 0）；也不要量自身宽度反推子元素尺寸（循环依赖）。
- 分支 fix/heatmap-restore-original，commit 64c0e16，--no-ff 合并 main（757b103），重新打包重启（PID 48911）。
- 备注：用户原始需求「占满宽度」尚未实现；若重做需先让容器能撑满宽度（如加 Spacer/固定 frame）再量宽。

## PERF-5 统计页缓存 + 复用 DateFormatter（fix/perf5-stats-cache，已合并 main）
- 实测问题比原 review 描述更严重：`heatmapCells(weeks:13)` 一次渲染触发 91 次 `count(for:)`，而 `dateString` 每次都 `DateFormatter()` 新建（Swift 里昂贵操作）。加上 `totalCount` 每次全量快照 `UserDefaults`，合计约 99 次 UserDefaults 读 + 99 次 DateFormatter 新建 + 1 次 `dictionaryRepresentation()`；且每次成功听写 `objectWillChange.send()` 触发重绘会再来一遍。
- 改法（`Sources/DictationStats.swift`）：复用单个 `static dayFormatter`；新增按天失效的内存缓存 `counts: [String: Int]`（跨天/写入/清理后失效重建）；`count(for:)`、`totalCount` 改读缓存；`cleanupOldEntriesIfNeeded` 改遍历缓存键集合，不再全量快照；键前缀收敛为单一 `static keyPrefix`（原先实例 `prefix` 与 `dayKey` 两处硬编码）。
- 正确性要点：`record()` 保持「写 UserDefaults → 清理 → invalidateCountsCache()」顺序，保证后续读（含日志里的 `todayCount`）拿到新值；清理用可能略旧的缓存键集合是安全的，因为只删除 90 天前、绝不会删当天键。
- 分支 fix/perf5-stats-cache，commit 7364b95，--no-ff 合并 main（cb5c0bb），重新打包重启（PID 49915）。
- 剩余：PERF-6（日志每条 open/close）、PERF-7（限流器 release fire-and-forget）、PERF-8（HUD TimelineView 每帧重绘）、P2-2（API Key 迁 Keychain）、P2-8（MainActorEventSink 事件投递顺序）。
