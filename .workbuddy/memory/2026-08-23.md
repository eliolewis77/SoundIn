
# 2026-08-23

## 图标定稿（08:57）
- 用户确认等间距版本（间距 48 = 条宽，整体居中）。iconutil 打包 icns、重建应用重启（PID 87091），AppIcon.svg 底稿同步更新。SoundIn 更名 + 图标至此定稿。

## 状态栏图标品牌化（15:53）
- MenuBarExtra label 改为：idle 态显示 `VoiceScribeApp.brandMenuBarIcon`（代码绘制的 NSImage 模板图，18×18pt，三根声波条 h=9/13/16.5 宽2.6 等间距 x=0.4/5.4/10.4 + 细光标 w1.6 @x15.4，isTemplate=true 自适应深浅色）；其余状态保留 SF Symbol（mic.fill 等）传达实时状态。
- 坑：`NSImage(size:)` 尾闭包会匹配到 `init(cgImage:size:)` 报错，须写全 `NSImage(size:flipped:false)`。
- 构建成功并重启（PID 89739）。待用户确认菜单栏视觉效果。

## 状态栏图标 v2（08-23 09:15）
- 用户反馈：少一条竖线（logo 是 5 元素）+ 图标偏大。重画 brandMenuBarIcon：按 gen_icon.py 几何比例 1024→16pt（k=0.03125）缩放，三根声波条（h 5/9/13pt，alpha 1/0.8/0.55 渐弱）+ 输入块 w2.5 + 细光标 w1.25，等间距 1.5，整体居中。模板图 alpha 即遮罩，渐弱效果保留。
- 构建重启（PID 95320），待用户确认。

## 菜单文案微调（08-23 09:17）
- 菜单栏右键菜单「退出 SoundIn」→「退出」（用户要求不带应用名）。构建重启 PID 95855。
- 通用设置 Toggle「登录时启动 SoundIn」→「开机时启动」。构建重启 PID 97168。

## 新功能规划（09:30）
- 用户确定按顺序做三项：①取消录音 ②替换选中内容 ③每日听写字数统计。
- 已出设计稿 design/mockup-cancel-replace-stats.html 待确认：
  ① Esc 取消（仅录音中生效，转写中不响应），胶囊右侧「ESC 取消」提示 + 取消后红✕「已取消」0.8s 消失，不粘贴不计字数
  ② 开始录音时 AX API 读焦点应用选区，有选中→胶囊上方绿色提示条「将替换选中的内容」，松手 ⌘V 原生覆盖选中；无选中照常插入
  ③ 通用页加「今日听写 N 字 · 本周 N 字」行，粘贴成功后计数（去空白字符），UserDefaults 按日键滚动、清理 90 天前
- 待用户确认后按序开发。

## 三项新功能实现完毕（09:40）
- ①取消录音：VoiceInputPhase/.cancelled + SpeechManager.cancelSession()（stopRecording+resetSession）；HotkeyInputManager.handleKeyEvent 最前面拦截录音中 Esc（!isARepeat），cancelRecording() 清理会话并上报 .cancelled；HUD 加 cancelled 态（红✕圆 + 「已取消」0.8s 消失）+ 录音阶段常驻「ESC 取消」小字。
- ②替换选中内容：HotkeyInputManager.detectSelectedText() 走 AX systemWide→focused element→kAXSelectedText，fallback kAXSelectedTextRange 长度>0。startRecording 时检测并写入 VoiceInputHUDManager.willReplaceSelection；HUD 窗口加高到 168×78（胶囊贴底位置不变），录音时上方浮绿色「将替换选中的内容」提示条；粘贴逻辑不变（⌘V 原生覆盖选中）。
  - 坑：macOS 26 SDK 的 AXUIElementCopyAttributeValue 只剩 3 参数（error 出参已移除），旧 4 参写法编译报 "extra argument in call"。
- ③听写统计：新建 Sources/DictationStats.swift（@MainActor 单例，键 dictationStats.yyyyMMdd，去空白字符计数，90 天清理，今日/本周口径）；stop() 里 paste 后调用 record（clipboardFallback 也计入）；通用页 Section("语音输入") 加 LabeledContent「今日听写 N 字 · 本周 N 字」。
- 构建重启 PID 7749，axTrusted=true。用户热键当前是纯修饰键 ⌥。三项待日常实测。

## ESC 提示修复（09:43）
- 用户反馈「ESC 取消」不显示：胶囊固定 168pt 宽，「语音输入」+分隔线+波形+提示超宽被裁。修复：录音阶段隐藏 Divider、水平内边距 16→13（showsEscHint 驱动）。构建重启 PID 9238。

## 统计独立页规划（09:48）
- 用户提出「今日听写」升级为独立设置页，想做成 GitHub 热力图或折线图。
- 已出设计稿 design/mockup-stats-page.html：A=13 周 GitHub 风格热力图（品牌绿梯度、悬停显示当日字数，推荐）+ 顶部今日/本周/累计(90天)三卡片；B=近 30 天折线图。待用户选方案后开发（SwiftUI Canvas 或 NSHostingView 实现，数据源 DictationStats 需加按日查询接口）。

## 统计页落地 + ESC 提示移除（09:51）
- 用户选方案 A；同时反馈胶囊上的「ESC 取消」文字不想要 → 已移除（Esc 取消功能保留，只是不再显示提示），布局还原 Divider + padding 16。
- 统计页落地：Page 枚举加 .stats（图标 chart.bar.fill，位于录音测试和关于之间）；statsPage = 三个 statBox（今日高亮绿底/本周/累计）+ Section("最近 13 周") DictationHeatmapView。
- DictationStats 新增：totalCount（所有键求和）、DayCell struct、heatmapCells(weeks:)（周一列对齐，最后一格=今天，未来日 date=nil）。
- 新文件 Sources/DictationHeatmapView.swift：14pt 格子 + 3pt 间距、月份标签行、星期标签列（一三五日）、.help() 原生悬停提示「M月d日 · N 字」、少→多图例；颜色四档品牌绿（D7ECC0/B4DD8A/97C459/6F9E3D），按 maxCount 比例分档。
- 构建重启 PID 13573。

## 月份标签重叠修复（09:56）
- 用户反馈「五月」「六月」挤在一起：网格首列只含月末一两天，5月标签和6月标签仅隔 1 列。修复：monthLabels 加最小间隔规则——与上一个已显示标签不足 2 列时跳过（GitHub 同款处理）。构建重启 PID 15437。

## 新三项功能规划（10:03）
- 用户确定顺序：①最近输入历史 ②麦克风选择 ③权限检查处加「去授权」按钮跳系统设置。
- 已出设计稿 design/mockup-history-mic-permissions.html：
  ① 菜单栏下拉顶部「最近输入」区，最多 10 条（截断一行+时间），点击=复制剪贴板（不直接粘贴），含清空历史；状态行加「今日 N 字」；UserDefaults 存储去重置顶
  ② 识别引擎页加「麦克风」Picker：跟随系统 + 枚举输入设备，拔设备回落默认，配置持久化
  ③ 关于页未授权权限行右侧绿色「去授权」按钮，NSWorkspace 打开 x-apple.systempreferences 对应面板（麦克风/语音识别/辅助功能）
- 待确认后开发。

## 三项功能落地（10:12）
- 用户调整：最近输入历史放设置→统计页（不放菜单栏）。
- ①InputHistory.swift（@MainActor ObservableObject，UserDefaults JSON key=recentInputHistory，max 10 条去重置顶，timeText 今天HH:mm/昨天/M月d日）；粘贴成功后与统计同点 record；统计页加「最近输入」Section（文字截断+时间+复制 link 按钮+清空历史 destructive），SettingsView 加 @ObservedObject history。
- ②麦克风选择：macOS 无 AVAudioSession（本 SDK 标记 unavailable）→ 改用 CoreAudio：AudioUnitSetProperty(inputNode.audioUnit, kAudioOutputUnitProperty_CurrentDevice) 绑设备；UID→AudioDeviceID 用 kAudioHardwarePropertyTranslateUIDToDevice（新 SDK 改名，旧名 TranslateStringToDeviceObject 已不存在；找不到返回 kAudioObjectUnknown 不报错）。SpeechManager.selectedMicrophoneUID 持久化，startRecordingSafe 里 startRecording 前调 applyPreferredInputDevice()。识别引擎页 Picker：跟随系统("")+AVCaptureDevice.devices(for:.audio)。需 import CoreAudio。
- ③关于页 permissionRow 加 panel 参数（未授权时显示「去授权」link 按钮），openSettingsPanel 用 NSWorkspace 打开 x-apple.systempreferences:com.apple.preference.security?Privacy_Microphone/SpeechRecognition/Accessibility；footer 文案同步更新。
- 构建重启 PID 25461。三项待实测。

## Git 仓库初始化（10:22）
- 项目此前无版本控制。git init + .gitignore 补 /.build-cache 排除；首次提交 f2aaddf（main 分支，30 文件，4890 行），含 Sources/Resources/design/.workbuddy 记忆日志。后续功能迭代按规范提交。

## 方案 C：静默成功（10:33）
- 用户讨论后确认：去掉成功后收缩绿圆对勾动画（reme 遗产，输入法场景节奏太拖）。改为转写完成立即粘贴，胶囊随 .idle 同步淡出；失败/剪贴板兜底仍显式提示。内容落地提前约 1 秒。
- 实现：stop() 删除 .success 上报 + 1150ms/600ms sleep，粘贴前直接发 .idle；HUD 的 VoiceInputHUDPhase 删掉 .success case，CapsuleView 删 isSuccess/successCheckVisible/backgroundShape 渐变，背景固定黑色 82%。VoiceInputPhase（app 级）保留 .success case 但不再发出。
- 提交 6a8cbc7。待用户体验反馈——若觉得缺确认感可回退或改 B（0.3s 对勾快闪）。
- 回归修复：删 success 代码时误删胶囊 `.frame(width:168, height:46)`，识别中进度条 GeometryReader 无约束撑满窗口导致胶囊变高。补回固定 frame（ZStack 层），HStack 不再单独设 height。提交 d36093e。
- 胶囊整体缩小一号（用户要求）：168×46 → 148×40，圆角 20，标题字 12，波形区 24×14，百分比框 32，图标 14/11pt，padding 14，HStack spacing 8；窗口 148×72。提交 be663d3。
- 左右空白优化：布局改为标题靠左 + Spacer + 分隔线/状态靠右的满宽排布，宽度再收 148→136（窗口 136×72）。提交 4b9cd5e。
- 胶囊标题左内边距 +2pt。提交 1d3fc6d。
- 流式实时预览讨论中：本机引擎已有增量 partials（SFSpeechRecognizer 回调实时更新 transcribedText，SpeechManager:1084）；API 分段模式有 completedSpeechSegments + segmentTranscriptionTasks，段级结果可能已可增量获取。待出 UI 方案。

## 流式实时预览落地（10:59）
- 用户定稿：方案 A、不加"…"、需独立开关。
- SpeechManager：livePreviewEnabled（UserDefaults 持久化，默认关）+ livePreviewText + livePreviewSegments；本机引擎 partial 回调里 updateLivePreview；API 分段在 queueSegmentTranscription 的 Task 完成时按段号合并（mergeAdjacentTranscription）；录音启动与 resetSession 均清空。
- HUD：窗口加宽 280×72（透明区不占视觉），胶囊宽度动态 136→264（按 NSString 测量文本宽 + chrome），标题区替换为尾部文本（无省略号，超宽 dropFirst），.animation(easeOut 0.15) on width。桥接 extension 加 livePreviewProxy/livePreviewEnabledProxy。
- 通用页加 Toggle「录音时实时预览转写」（开启时显示说明文字）。
- 提交 5f5a913，重启 PID 50860。待实测两种引擎的预览效果。
- 回归：加开关时误删触发方式 Picker 的闭合括号，Toggle 被嵌进 Picker 内部不渲染（编译仍通过！）。修复括号后开关正常显示。提交 1d4d8b6。教训：SwiftUI ViewBuilder 嵌套错位不一定报错，改完要在真机确认 UI。
- API 引擎预览无输出的根因：SegmentedAudioFileWriter 段最短 8s + 静音才切，短句整段到松手才关闭 → 录音中无结果。改法：开启预览时 setPreviewRotationInterval(4)，shouldRotateSegmentLocked 忽略最短段限制按 4s 强切；最终转写仍走完整音频（finish 时 <20s 会丢弃分段重转全文，质量不受影响，代价是预览 chunk 多几次 API 调用）。提交 6a5ef4f，重启 PID 58431。

## 流式实时预览回退完成（15:55）
- 三个 revert 全部提交：09e5aaa（API 强切段）、1bd3828（Picker 括号修复）、15d010e（预览主体）
- 代码验证无残留：grep livePreview/previewRotation 零匹配；HUD 窗口恢复 136×72、胶囊 136×40
- 构建通过（5.92s），应用已重启（pid 62848）
- 设计结论留存备后续重启该功能：方案 A、无省略号、独立开关；API 引擎需每 4s 强切预览段

## API 多配置档功能（feat/api-profiles 分支）
- 新增 Sources/APIProfiles.swift：APIProfile + APIProfileStore（JSON 存 vs.apiProfiles，engine/polish 独立选中 ID）
- 迁移逻辑：旧三个字段打包「默认」档；润色配置不同则加「默认（润色）」档
- 设置页：识别引擎/文字优化区块顶部加接口配置 Picker，字段双写 Binding（改即存回档），添加（alert 命名）/删除（confirmationDialog）按钮
- 启动时 AppDelegate 调 applyActive 回写活动值；菜单栏快捷切换（设计稿③）暂未做，待用户确认
- 提交 0280cc1，构建通过，应用已重启（pid 31659），等用户验证后合并 main

## feat/api-profiles 合并完成（14:05）
- 用户验证通过，--no-ff 合并回 main（75752ca），分支保留
- main 重新打包重启（pid 39827）
- 本批功能：API 多配置档（识别/润色独立选择+迁移）、测试连接结果下移、局域网首连瞬时失败自动重试、录音测试页提示语下移
- 未做：菜单栏「识别配置」快捷切换子菜单（用户未确认要）

## 代码审查修复批次（fix/code-review-20260823 分支，21:15）
- 独立 review 报告存档 design/code-review-2026-08-23.md（双 subagent 并行审查+人工核实）
- 已落盘修复：P0-1 startRecordingSafe 兜底复位 isStarting；P0-2 APIProfiles init 解码失败走迁移+删旧键+日志+选中项兜底；P1-1 菜单栏补 voicePhaseChanged 订阅；P1-2 resetLocked/resetSession 补删临时 wav；P1-3 finishSegmentedTranscription 失败段重试一次+占位标记+transcribeSegment 日志常开；P1-4 权限弹窗期间会话挂起（SpeechManager notDetermined 挂起 + requestPermissions 拒绝分支异步上报 + 热键路径同步）；P1-5 HUD 不可见不吞失败上报；P1-6 DictationStats 改 ObservableObject，record() 发 objectWillChange，SettingsView 加 @ObservedObject stats 订阅 → 统计页实时刷新
- 构建通过（6.15s，仅 3 个既有警告），打包 SoundIn.app 已重启（pid 11191）
- 用户指示：暂不提交不合并，修复改动留工作区待命
- 挂账：P1 主线程 multipart 大重构（下轮）；菜单栏识别配置快捷切换子菜单（未确认）；P2 项（SettingsView 拆分、HUD 尺寸常量、多屏定位、Keychain）
- 验证通过（用户实测：Esc 取消后临时目录不再新增文件，P1-2 生效），提交 51a9019（9 files +354），--no-ff 合并回 main 3e5e8f7，main 重新打包重启
- 旧残留（8/22 的 segment-0.wav，1.9MB）仍在 $TMPDIR，可手动删；TMPDIR=/var/folders/kn/3b16wc6n37l65vzm_zbmn6vm0000gn/T/

## P2 修复批次（fix/p2-batch-20260823 分支，21:40）
- 用户选定 6 项：P2-1、P2-3、P2-6、P2-7、P2-9、P2-10
- P2-1 stopRecordingAndWaitForText 去掉 1.2s 固定超时 + 0.35s 静默截断，改为只认 recognitionDidFinish + 8s 兜底，50ms 轮询粒度（原计划 continuation 竞速，遇到 Swift 6 区域隔离检查器 withTaskGroup 内 @MainActor 闭包的编译器 bug，改等效轮询方案）
- P2-3 SettingsView 抽公共组件：profileFieldsSection（三字段+缺字段提示）+ profileActionRow（添加/删除/测试+结果），引擎页/润色页复制体替换
- P2-6 清空历史加 confirmationDialog；配置字段/切换档位后 engineConnectionTest/polishConnectionTest 失效清空
- P2-7 HUD 定位改用鼠标所在屏（NSEvent.mouseLocation + NSMouseInRect，兜底 NSScreen.main）
- P2-9 HUD 尺寸收敛：windowSize 去 private，capsule 视图引用 VoiceInputHUDManager 常量，胶囊高 40 用 capsuleHeight
- P2-10 HotkeyFileLog 5MB 轮转（moveItem 归档 .old，注意 FileManager 无 renameItem）；case .failure(let _) 清理
- 构建通过（6.27s，3 个既有警告），应用已重启（pid 21571），待用户验证后提交合并
- 遗留未做：P2-2 API Key 迁 Keychain、P2-4 detectSelectedText 后台化、P2-5 粘贴判定 sleep、P2-8 MainActorEventSink 顺序、P1-7 主线程 multipart 重构
- 用户验证通过 + 追加反馈：添加配置按钮去掉省略号（→「＋ 添加配置」）
- 提交 31c6624（5 files +157/-101），--no-ff 合并回 main dbdb4cb，main 重新打包重启

## HUD 绿色残留收缩修复（fix/hud-phase-reset 分支，21:52）
- 用户反馈：长按录音胶囊显示时，绿色从中间快速向左收缩后消失（误以为进度条出现在录音阶段）
- 根因：hide() 只淡出窗口不复位状态，phase 停在 .transcribing（progress≈0.9）；下次 show() 切 .recording 时，SwiftUI .animation(value: progress) 给 0.9→0 跳变播放 0.12s 收缩动画 → 视觉上绿色快速向左收缩
- 修复：hide 淡出完成（orderOut 后）复位 phase/progress/willReplaceSelection；showGeneration 代际计数防旧 hide 完成回调误藏新胶囊（含 update 失败提示路径）
- 构建通过，应用已重启（pid 27083），待用户验证后合并

## 静音幻觉语气词过滤（fix/filler-filter 分支，21:56）
- 用户反馈：按热键不说话直接松手，会粘贴出"嗯。"（OpenAI 兼容 Whisper 类模型对静音音频的幻觉输出）
- 修复：HotkeyInputManager.isEffectivelyEmpty 判定（空/纯标点/纯语气词 嗯啊呃哦诶哎唔 em um hmm hm en oh），热键粘贴路径在 polish 之前过滤，命中则跳过粘贴与统计，日志记 filtered empty-ish result
- 录音测试页保持显示原始转写（便于观察模型行为，不经过此过滤）
- 构建通过，应用已重启（pid 29312），待用户验证后合并
- 用户追加方案：静音时根本不走转写请求（比事后过滤更彻底）
- 实现：SpeechManager 加 hasDetectedSpeech（audioLevel didSet 连续 3 帧 ≥0.10 判定人声，startRecording 重置），stopRecordingAndWaitForText 在 stopRecording 后短路返回 ""（不调 API，日志 no speech detected）；isEffectivelyEmpty 过滤保留作双保险
- 录音测试页文案区分静音（未检测到声音）/识别失败（未识别到内容）
- 构建通过，应用已重启（pid 30546），待用户验证后合并

## P2-4 + P2-5 修复（fix/p2-4-p2-5 分支，22:09）
- P2-4：detectSelectedText 标 nonisolated 并改为后台线程执行（Task.detached，结果回主线程按 sessionID 校验后更新 willReplaceSelection）；抽出非主线程安全的 focusedSelectedText() 复用
- P2-5：paste() 固定 sleep 320ms 改为 waitForPasteEffect 自适应——AX 可用且应用暴露选中文本时轮询（80ms×）直到包含转写内容（上限 1.5s）；无 AX 权限/不暴露选中文本时 320ms 兜底保持旧时序
- 构建通过（6.21s），应用已重启（pid 32122），待用户验证后合并
- 用户追加反馈：替换选中文本时句尾多一个句号（识别模型 Whisper 自动标点 / polish 书面语化）
- 方案 A：仅替换选中文本场景（willReplaceSelection）去掉末尾单个"。"，普通输入保留；加在 polish 之后、粘贴之前
- 构建通过，应用已重启（pid 32887）；本轮 fix/p2-4-p2-5 分支累计 P2-4/P2-5 + 方案 A 三项待用户验证后合并
- 句号问题多轮排查结论：用户测试应用不暴露 AX 选区（paste: no AX selection readable，frontmost PID 主线程读取 + 0.3s messaging timeout 均无效），标点跟随逻辑保留但该场景不生效（属应用限制）；用户决定搁置
- fix/p2-4-p2-5 合并 main 16efa65（P2-4 主线程 Task+超时 AX、P2-5 waitForPasteEffect、标点跟随），main 打包重启
- 剩余挂账：P1-7 主线程拼 multipart、P2-2 Keychain、P2-8 事件投递顺序；菜单栏识别配置快捷切换（未确认）
